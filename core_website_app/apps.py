""" Apps file for setting core package when app is ready.
"""

from django.apps import AppConfig


class CoreWebsiteAppConfig(AppConfig):
    """Core application settings."""

    name = "core_website_app"
    verbose_name = "Core Website App"
